const express = require('express');
const router = express.Router();

// Placeholder route, can be removed later
router.get('/', (req, res) => {
    res.send('User routes placeholder');
});

module.exports = router;
